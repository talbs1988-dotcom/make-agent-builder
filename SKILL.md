---
name: make-agent-builder
description: אפיון, תיעוד ובנייה של סוכני AI ואוטומציות ב-Make.com. מריץ אפיון מונחה מול בעל העסק (מה אתה רוצה שהסוכן יעשה → איזה כלי → איזה מידע), מפיק מסמך ארכיטקטורה ב-15 סעיפים, ואז בונה את התרחיש בפועל דרך ה-MCP של Make — או מפיק Blueprint לייבוא כשאין חיבור. השתמש בסקיל הזה בכל פעם שמבקשים "תבנה לי סוכן ב-Make", "סוכן שעונה ללידים בוואטסאפ", "תנתח לי את התרחיש", "תתעד את האוטומציה", "תבנה מחדש את התרחיש", "למה התרחיש נופל", "תמפה לי את המודולים", או כשבונים סוכן שירות לקוחות / ניהול לידים על וואטסאפ עסקי רשמי. חל גם על תיקון תרחיש קיים ועל הפקת בלופרינט לתלמידים.
---

# בניית סוכני AI ב-Make

## ⛔ ארבעה כללים שלא נשברים

1. **אל תנחש שם של מודול, שדה או ערך.** לפני כל מודול — `app-modules_list` ואז `app-module_get`.
2. **הרץ `validate_module_configuration` על כל מודול לפני שיוצרים תרחיש.** זה מחזיר בדיוק אילו שדות חסרים. תרחיש שנוצר בלי זה ייפול ב-`BlueprintValidationError` ברגע שמנסים להריץ אותו — והמשתמש יראה מסך מלא אדום.
3. **"נוצר" ≠ "עובד".** תרחיש שנוצר בהצלחה יכול להיכשל באימות. הבדיקה היחידה שנחשבת היא ריצה אמיתית שמחזירה תוצאה.
4. **אל תיגע בתרחיש קיים של המשתמש.** בונים עותק או תרחיש חדש. שואלים לפני כל שינוי.

## שלב 0 · לאיזה מצב נכנסים

בדוק אם יש חיבור MCP ל-Make (`users_me`).

- **יש חיבור** → אפשר לבנות ישירות בחשבון של המשתמש.
- **אין חיבור** → מפיקים תכנון, מיפויים ו-Blueprint לייבוא. **לא מבטיחים שינויים בתוך Make.**

אמור למשתמש באיזה מצב אתם, במשפט אחד, לפני שמתחילים.

## שלב 1 · אפיון — מפעולה, לכלי, למידע

**אל תשאל שאלה פתוחה.** בעל עסק לא יודע מה אפשר לבקש. הצג רשימה שהוא מזהה מהיום-יום שלו והוא מסמן.

**השאלה הפותחת:** *מה אתה רוצה שהסוכן יעשה בשבילך?* — כרשימת בחירה, לא כטקסט חופשי.

ואז תרגם כל פעולה שסימן לארבע עמודות:

| מה שביקשת | הכלי שהוא צריך | המידע שהכלי צריך | מה יקרה בלי זה |

**שני כללים שנגזרים מהטבלה:**
- **מקור מידע אחד משרת כמה פעולות.** אוספים קודם את כל הפעולות, ורק אחר כך מרכיבים את המידע — אחרת בונים שלוש טבלאות במקום אחת.
- **פעולה שאין לה כלי ואין לה מידע היא לא יכולת של הסוכן. היא סיפור שהוא יספר ללקוח.**

### חמש משפחות הכלים — צ'קליסט פנימי, לא שאלה למשתמש

| # | המשפחה | תפקיד | חובה? |
| --- | --- | --- | --- |
| 1 | **ידיעה** | קורא בלבד: טבלת שירותים, מאגר ידע | כן, לפחות אחד |
| 2 | **זיכרון** | מצא רשומה **ואז** צור/עדכן | כן, **שניים** |
| 3 | **פעולה** | פגישה, קישור תשלום, חשבונית | לפי האפיון |
| 4 | **גבול** | העברה לאדם + סימון הרשומה | **כן, תמיד** |
| 5 | **דיבור** | שליחת התשובה | כן, **אחד בלבד** |

⚠️ בלי כלי "מצא" — כל הודעה פותחת רשומה חדשה, וזה יומן הודעות ולא CRM.
⚠️ שני כלי דיבור = הלקוח מקבל את התשובה פעמיים.

### מה חייבים לקבל מהמשתמש ואי אפשר להסיק

לבקש במפורש, בטופס: **הכלים שכבר קיימים אצלו ולמה כל אחד משמש** — קישור לגיליון, מזהה טבלה, יומן, קבצי ידע, קישורי תשלום, כתובת מייל להתראות. ולכל אחד: **קריאה בלבד או גם כתיבה.**

## שלב 2 · שלוש שכבות ההנחיה

| השכבה | איפה | על מה עונה |
| --- | --- | --- |
| **1 · פרומפט ראשי** | System Prompt של הסוכן | **איך הוא מתנהג** — תפקיד, מטרה, למי עונה, איך מדבר, מה אסור, ואיך סוגר הודעה |
| **2 · הנחיה בכל כלי** | שדה Description של הכלי | **מתי הוא פועל** — מתי לגשת, מה לבדוק, מה לעשות עם התוצאה |
| **3 · קבצי מידע** | מאגר הידע | **מאיפה הוא יודע** — התשובות עצמן |

**הכלל שמחלק:** מידע שמשתנה — מחיר, מועד, קישור — **לא** נכנס לפרומפט. הוא נכנס לטבלה או לקובץ. פרומפט משנים בעריכה ובסיכון, טבלה משנים בלי לגעת בסוכן.

**וכלל שני:** הפרומפט הראשי אומר **מתי**. ההנחיה בכלי אומרת **איך**. מי שדוחס את שניהם לפרומפט מקבל מגילה שהמודל מדלג עליה.

## שלב 3 · מסמך הארכיטקטורה

לפני בנייה, הפק מסמך לפי המבנה ב-`references/architecture-format.md` — 15 סעיפים, מתקציר ועד "מידע שחסר". אל תשאל שאלות שאפשר להסיק מהתרחיש או לבדוק בכלי חיבור.

חמש שכבות שהמסמך חייב לכסות: **הפעלה · איסוף נתונים · עיבוד והחלטות · פעולות · אמינות ותפעול.**

## שלב 4 · בנייה

לפני `scenarios_create` — עבור על `references/make-gotchas.md`. שם יושבות המלכודות שכבר עלו בדם.

סדר העבודה:
1. `app-modules_list` לכל אפליקציה · `app-module_get` לכל מודול שמשתמשים בו
2. `validate_module_configuration` לכל מודול, עד `valid: true`
3. `connections_list` — להשתמש במזהי חיבור אמיתיים, לא להמציא
4. `scenarios_create` עם `folderId` נכון
5. ריצת בדיקה אמיתית, וקריאת ה-execution

## שלב 5 · בדיקה חיה

**"התרחיש נדלק" זו לא בדיקה.** בדיקה היא: אירוע אמיתי אחד נכנס, ואתה בודק שלושה דברים — הגיע, נשמר במקום הנכון, והתשובה חזרה. ואז קורא את ה-execution ומדווח מה עבד ומה לא.

אם הריצה נכשלה — `executions_get-detail` **לפני** שנוגעים שוב בתרחיש.
